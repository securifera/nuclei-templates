id: flock-safety-camera-panel

info:
  name: Flock Safety Camera Admin Panel - Detect
  author: inokii
  severity: info
  description: |
    Detected the Flock Safety camera admin panel.
  reference:
    - https://www.flocksafety.com/products/video-cameras
  classification:
    cpe: cpe:2.3:a:flock-safety:flock-safety:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.html:"Flock Admin"
    vendor: flock-safety
    product: flock-safety
  tags: panel,iot,camera,flock-safety,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        name: flock-safety
        cpe: cpe:2.3:a:flock-safety:flock-safety:*:*:*:*:*:*:*:*
        part: body
        words:
          - "Flock Admin</title>"
          - "com.flocksafety.android.cameraconfig"
          - "com.flocksafety.android.videorecording"
        condition: or

      - type: status
        status:
          - 200
