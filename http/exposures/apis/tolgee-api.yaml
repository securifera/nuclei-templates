id: tolgee-api

info:
  name: Tolgee API - Detect
  author: matejsmycka
  severity: info
  description: Tolgee API was detected.
  reference:
    - https://docs.tolgee.io/api
  classification:
    cpe: cpe:2.3:a:tolgee:api:*:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    verified: true
    shodan-query: html:"tolgee"
    vendor: tolgee
    product: api
  tags: exposure,api,tolgee,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/api/public/configuration"

    matchers-condition: and
    matchers:
      - type: word
        name: api
        cpe: cpe:2.3:a:tolgee:api:*:*:*:*:*:*:*:*
        part: body
        words:
          - Tolgee
          - version
        condition: and

      - type: word
        name: api-2
        cpe: cpe:2.3:a:tolgee:api:*:*:*:*:*:*:*:*
        part: content_type
        words:
          - application/json

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        regex:
          - '"version"\s*:\s*"([^"]+)"'
        group: 1
        name: version
