id: fork-installer

info:
  name: Fork CMS - Installer
  author: DhiyaneshDk
  severity: critical
  description: |
    Fork CMS installer page was detected.
  classification:
    cpe: cpe:2.3:a:fork:fork:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    fofa-query: body="Install Fork CMS"
    vendor: fork
    product: fork
  tags: misconfig,exposure,fork,cms,install,vuln

http:
  - method: GET
    path:
      - '{{BaseURL}}/install/2'

    matchers-condition: and
    matchers:
      - type: word
        name: fork
        cpe: cpe:2.3:a:fork:fork:*:*:*:*:*:*:*:*
        part: body
        words:
          - "Install Fork CMS"
          - "Settings"
        condition: and

      - type: status
        status:
          - 200
