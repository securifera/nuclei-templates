id: krayin-installer

info:
  name: Krayin CMS - Installer
  author: theamanrawat
  severity: high
  description: |
    Krayin CRM installer page was detected.
  classification:
    cpe: cpe:2.3:a:krayin:krayin:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    fofa-query: body="Krayin Installer"
    vendor: krayin
    product: krayin
  tags: misconfig,exposure,krayin,crm,install

http:
  - method: GET
    path:
      - '{{BaseURL}}/install'

    matchers-condition: and
    matchers:
      - type: word
        name: krayin
        cpe: cpe:2.3:a:krayin:krayin:*:*:*:*:*:*:*:*
        part: body
        words:
          - "<title>Krayin Installer"
          - "Welcome to Installation"
        condition: and

      - type: status
        status:
          - 200
