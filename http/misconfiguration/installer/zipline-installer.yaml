id: zipline-installer

info:
  name: Zipline - Installer
  author: pussycat0x
  severity: critical
  description: |
    Zipline installer setup was detected.
  reference:
    - https://zipline.diced.sh/docs/get-started/docker
  classification:
    cpe: cpe:2.3:a:zipline:zipline:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: zipline
    product: zipline
  tags: misconfig,setup,zipline,installer,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/setup"

    matchers-condition: and
    matchers:
      - type: word
        name: zipline
        cpe: cpe:2.3:a:zipline:zipline:*:*:*:*:*:*:*:*
        part: body
        words:
          - "Setup Zipline"
          - "Configuration"
          - "Create a super-admin account"
        condition: and

      - type: status
        status:
          - 200
