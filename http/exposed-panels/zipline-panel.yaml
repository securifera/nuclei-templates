id: zipline-panel

info:
  name: Diced Zipline - Detect
  author: icarot
  severity: info
  description: |
    Zipline panel was detected.
  reference:
    - https://github.com/diced/zipline
  classification:
    cpe: cpe:2.3:a:diced:zipline:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: diced
    product: zipline
    shodan-query: html:"Zipline"
  tags: diced,zipline,login,panel,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/auth/login"

    matchers-condition: and
    matchers:
      - type: word
        name: zipline
        cpe: cpe:2.3:a:diced:zipline:*:*:*:*:*:*:*:*
        words:
          - '"title":"Zipline"'
          - 'Zipline – Login</title>'
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200
