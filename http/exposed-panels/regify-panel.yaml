id: regify-panel

info:
  name: Regify Login Panel - Detect
  author: righettod
  severity: info
  description: |
    Regify Login Panel was detected.
  reference:
    - https://www.regify.com/
  classification:
    cpe: cpe:2.3:a:regify:regify:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.favicon.hash:1817615343
    vendor: regify
    product: regify
  tags: panel,regify,login,detect,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}/phpLogin.php'

    matchers-condition: and
    matchers:
      - type: word
        name: regify
        cpe: cpe:2.3:a:regify:regify:*:*:*:*:*:*:*:*
        part: body
        words:
          - 'regify'
          - 'regibill'
          - 'regify_default.css'
        condition: and
        case-insensitive: true

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)appv=([0-9.-]+)'
