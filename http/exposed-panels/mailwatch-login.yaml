id: mailwatch-login

info:
  name: MailWatch Login Panel - Detect
  author: oppsec
  severity: info
  description: MailWatch login panel was detected.
  classification:
    cpe: cpe:2.3:a:mailwatch:mailwatch:*:*:*:*:*:*:*:*
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"MailWatch Login Page"
    vendor: mailwatch
    product: mailwatch
  tags: panel,mailwatch,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/mailscanner/login.php"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        name: mailwatch
        cpe: cpe:2.3:a:mailwatch:mailwatch:*:*:*:*:*:*:*:*
        part: body
        words:
          - "MailWatch Login"

      - type: status
        status:
          - 200
