id: quilium-panel

info:
  name: Quilium Panel - Detect
  author: righettod
  severity: info
  description: |
    Quilium CMS Login Panel was detected.
  reference:
    - https://www.quilium.io/
  classification:
    cpe: cpe:2.3:a:quilium:quilium:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 2
    shodan-query: http.html:"CMS Quilium"
    vendor: quilium
    product: quilium
  tags: panel,quilium,login,detect,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'
      - '{{BaseURL}}/en/login'

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: word
        name: quilium
        cpe: cpe:2.3:a:quilium:quilium:*:*:*:*:*:*:*:*
        part: body
        words:
          - 'content="CMS Quilium'
          - 'Quilium</a>'
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - 'CMS Quilium ([a-f0-9.]+)'
