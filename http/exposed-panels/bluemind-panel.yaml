id: bluemind-panel

info:
  name: Bluemind Panel - Detect
  author: Tigibus
  severity: info
  description: |
    Bluemind application panel was discovered.
  reference:
    - https://bluemind.net/
  classification:
    cpe: cpe:2.3:a:bluemind:bluemind:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"Welcome to BlueMind"
    vendor: bluemind
    product: bluemind
  tags: bluemind,login,panel,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/login/native"

    matchers-condition: and
    matchers:
      - type: word
        name: bluemind
        cpe: cpe:2.3:a:bluemind:bluemind:*:*:*:*:*:*:*:*
        part: body
        words:
          - 'alt="BlueMind"'
          - 'window.bmExtensions'
        condition: and

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        group: 1
        part: body
        regex:
          - 'window\.applicationVersion = "([0-9.]+)";'
