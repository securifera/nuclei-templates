id: interactsoftware-panel

info:
  name: Interact Software Panel - Detect
  author: righettod
  severity: info
  description: Interact Software was detected.
  reference:
    - https://www.interactsoftware.com
  classification:
    cpe: cpe:2.3:a:interactsoftware:interact:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: interactsoftware
    product: interact
  tags: panel,interactsoftware,interact,detect,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}/login'

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        name: interact
        cpe: cpe:2.3:a:interactsoftware:interact:*:*:*:*:*:*:*:*
        part: body
        words:
          - 'www.interactsoftware.com'
          - 'community.interact-intranet.com'
          - '/InteractV'
        condition: and

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?:v=)([0-9]+\.[0-9]+\.?[0-9]*\.?[0-9]*)'
