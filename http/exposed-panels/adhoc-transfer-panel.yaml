id: adhoc-transfer-panel

info:
  name: WS-FTP Ad Hoc Transfer Panel - Detect
  author: johnk3r
  severity: info
  description: WS_FTP Ad Hoc panel was detected.
  classification:
    cpe: cpe:2.3:a:progress:ws_ftp:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: progress
    product: ws_ftp
    shodan-query:
      - http.title:"Ad Hoc Transfer"
      - ws_ftp port:22
      - http.title:"ad hoc transfer"
      - http.title:"ws_ftp server web transfer"
    fofa-query:
      - title="ws_ftp server web transfer"
      - title="ad hoc transfer"
    google-query:
      - intitle:"ws_ftp server web transfer"
      - intitle:"ad hoc transfer"
    censys-query: services.http.request.uri="*/thinclient/wtm/public/index.html"
  tags: panel,wsftp,ad-hoc,detect,login,progress,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/AHT/"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        name: ws_ftp
        cpe: cpe:2.3:a:progress:ws_ftp:*:*:*:*:*:*:*:*
        part: body
        words:
          - 'Ad Hoc Transfer'

      - type: status
        status:
          - 200
