id: wordpress-official-facebook-pixel

info:
  name: Meta pixel for WordPress Detection
  author: ricardomaia
  severity: info
  reference:
    - https://wordpress.org/plugins/official-facebook-pixel/
  classification:
    cpe: cpe:2.3:a:official-facebook-pixel:official-facebook-pixel:*:*:*:*:*:wordpress:*:*
  metadata:
    plugin_namespace: official-facebook-pixel
    wpscan: https://wpscan.com/plugin/official-facebook-pixel
  tags: tech,wordpress,wp-plugin,top-200,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/wp-content/plugins/official-facebook-pixel/readme.txt"

    payloads:
      last_version: helpers/wordpress/plugins/official-facebook-pixel.txt

    extractors:
      - type: regex
        part: body
        internal: true
        name: internal_detected_version
        group: 1
        regex:
          - '(?i)Stable.tag:\s?([\w.]+)'

      - type: regex
        part: body
        name: detected_version
        group: 1
        regex:
          - '(?i)Stable.tag:\s?([\w.]+)'

    matchers-condition: or
    matchers:
      - type: dsl
        name: "outdated_version"
        cpe: cpe:2.3:a:official-facebook-pixel:official-facebook-pixel:*:*:*:*:*:wordpress:*:*
        dsl:
          - compare_versions(internal_detected_version, concat("< ", last_version))

      - type: regex
        name: official-facebook-pixel
        cpe: cpe:2.3:a:official-facebook-pixel:official-facebook-pixel:*:*:*:*:*:wordpress:*:*
        part: body
        regex:
          - '(?i)Stable.tag:\s?([\w.]+)'
