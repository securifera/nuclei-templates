id: web-suite-detect

info:
  name: Web Suite Detect
  author: pikpikcu
  severity: info
  classification:
    cpe: cpe:2.3:a:bqe:web_suite:*:*:*:*:*:*:*:*
  metadata:
    max-request: 3
    fofa-query: Web Suite 2021 Login
    vendor: bqe
    product: web_suite
  tags: tech,web-suite,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/ws2020/"
      - "{{BaseURL}}/ws2021/"

    matchers-condition: and
    matchers:
      - type: word
        name: web_suite
        cpe: cpe:2.3:a:bqe:web_suite:*:*:*:*:*:*:*:*
        part: body
        words:
          - 'www.bqe.com">BQE Software Inc.</a>'
          - 'Web Suite'
        condition: and

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - "Version: (.*)</span>"
