id: appdynamics-rum-detect

info:
  name: AppDynamics (Cisco) RUM - Detect
  author: Shivam Kamboj
  severity: info
  description: |
    Detects AppDynamics (Cisco) End User Monitoring (EUM) artifacts.
  reference:
    - https://docs.appdynamics.com/appd/24.x/latest/en/end-user-monitoring/browser-monitoring/browser-real-user-monitoring
  classification:
    cpe: cpe:2.3:a:cisco:appdynamics:*:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    verified: true
    shodan-query: html:"adrum"
    vendor: cisco
    product: appdynamics
  tags: tech,discovery,rum,appdynamics

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    redirects: true

    matchers-condition: and
    matchers:
      - type: word
        name: appdynamics
        cpe: cpe:2.3:a:cisco:appdynamics:*:*:*:*:*:*:*:*
        part: body
        words:
          - "adrum-config"
          - "adrum-start-time"
          - "adrumExtUrlHttp"
          - "adrumExtUrlHttps"
        condition: or

      - type: status
        status:
          - 200
